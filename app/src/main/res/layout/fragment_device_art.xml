<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                xmlns:tools="http://schemas.android.com/tools"
                android:orientation="vertical">

    <TextView
        android:id="@+id/fragment_device_art_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/fragment_device_art_image"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="@string/startup_wizard_alphabet_description"
        android:textColor="@color/textColorHeader"
        tools:textColor="@color/textColorPrimary"
        android:textSize="@dimen/activity_startup_wizard_text_size"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        />

    <ImageView
        android:id="@+id/fragment_device_art_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        tools:src="@drawable/device_art_lesson"
        android:adjustViewBounds="true"
        android:maxHeight="400dp"
        android:maxWidth="400dp"/>
</RelativeLayout>